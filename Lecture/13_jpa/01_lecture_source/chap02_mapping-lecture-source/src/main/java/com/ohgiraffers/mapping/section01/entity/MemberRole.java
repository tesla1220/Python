package com.ohgiraffers.mapping.section01.entity;

public enum MemberRole {

    /* 필기 : Enum (Enumerated) 타입
    *   Enum 타입을 사용하게 되면 코드의 가독성 향상
    *   Type safety (타입의 안정성)을 보장할 수 있다.
    *   서로 연관된 상수들의 집합 클래스이다. */
    // enum 타입은 상수. 대문자로작성
    ROLE_MEMBER,
    ROLE_ADMIN

}
